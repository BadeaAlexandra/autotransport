<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.parse.starter">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".StarterApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ticketlogo_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BookingsActivity"
                  android:label="Rezervari" />
        <activity android:name=".ProfileActivity"
                  android:label="Profil"/>
        <activity android:name=".ContactActivity"
                  android:label="Contact"/>
        <activity
            android:name=".OptionsCompleteActivity"
            android:label="AutoTransport"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".HelpActivity"
                  android:label="Centru de Asistenta"/>
        <activity android:name=".EmailActivity"
                  android:label="Finalizarea achizitiei"/>
        <activity android:name=".TermsConditionsActivity"
                  android:label="Termeni si Conditii"/>
        <activity android:name=".PurchaseActivity"
                  android:label="Detaliile de plata"/>
        <activity android:name=".SignUpActivity"
                  android:label="Inregistrare"/>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ResultsActivity"
                  android:label="Rezultate"
                  android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>